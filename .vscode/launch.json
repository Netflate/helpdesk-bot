{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Bot",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/bot/main.py"
        },

        {
            "name": "Run Site",
            "type": "go",
            "request": "launch",
            "program": "${workspaceFolder}/site/cmd/server/main.go"
        }
    ]
}
